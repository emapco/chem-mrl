name: Run Tests

on:
  push:
    branches:
      - main
      - "releases/**"
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    timeout-minutes: 150
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip

          # Extract torch version from pyproject.toml and install CPU version
          TORCH_VERSION=$(python -c "import tomllib; f=open('pyproject.toml','rb'); data=tomllib.load(f); deps=[d for d in data['project']['dependencies'] if d.startswith('torch')]; print(deps[0].split('torch')[1] if deps else '')")
          pip install "torch${TORCH_VERSION}" --index-url https://download.pytorch.org/whl/cpu

          pip install .[dev,train]

      - name: Run tests with pytest
        run: pytest tests -v
